# 正式環境相依套件（Render 部署用）
Django==5.2.16
django-unfold==0.100.0        # 後台外觀
whitenoise==6.12.0            # 正式環境提供靜態檔（CSS/JS）
Pillow==12.2.0               # ImageField 影像處理
openpyxl==3.1.5              # 後台「全欄位匯出」產生 .xlsx（write_only 串流）
gunicorn==23.0.0             # 正式環境 WSGI 伺服器

# ---- 資料庫（PostgreSQL）----
psycopg2-binary==2.9.10      # PostgreSQL 驅動
dj-database-url==3.1.2       # 解析 Render 提供的 DATABASE_URL

# ---- 媒體檔外部儲存（Cloudinary；Render 磁碟會掉檔）----
cloudinary==1.45.0
django-cloudinary-storage==0.3.0
